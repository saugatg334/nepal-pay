# Fix AuthController Helper Functions Redeclaration

- [x] Protect csrf_token(), verifyCsrf(), flash(), getFlash(), redirect() with if (!function_exists())
- [x] Run PHP lint on AuthController.php ✓ No syntax errors
- [x] Test login/register routes
- [x] Complete task

